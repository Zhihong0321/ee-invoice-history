{
  "name": "ee-invoice-history",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone read-only viewer for invoice_audit_log",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node --check server.js && node --check src/db.js && node --check src/repo/history.js && node --check src/repo/viewerActivity.js && node --check src/routes/api.js"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
